<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>Request Details - Admin Dashboard</title>
  <link rel="stylesheet" href="vendors/simplebar/css/simplebar.css">
  <link href="css/style.css" rel="stylesheet">
  <style>
    .main-content { margin-left: 250px; padding: 20px; }
    .card { margin-bottom: 20px; }
    .section-title { font-size: 1.5rem; margin-bottom: 15px; }
    .stat-card { background-color: #f7f7f7; padding: 20px; border-radius: 10px; text-align: center; }
    .queue-list { list-style: none; padding: 0; }
    .queue-list li { padding: 10px; border: 1px solid #ddd; margin-bottom: 5px; cursor: pointer; }
    .queue-list li:hover { background-color: #f0f0f0; }
    .quick-action-link { display: block; width: 100%; text-align: center; }
  </style>
</head>
<body>
  <div class="sidebar sidebar-dark sidebar-fixed" id="sidebar">
    <div class="sidebar-brand"><h4>Admin Dashboard</h4></div>
    <ul class="sidebar-nav">
      <li class="nav-item"><a class="nav-link" href="/dashboard">Dashboard</a></li>
      <li class="nav-item"><a class="nav-link" href="#">Requests</a></li>
      <li class="nav-item"><a class="nav-link" href="#">Admin Settings</a></li>
    </ul>
  </div>

  <header class="header header-sticky mb-4">
    <div class="container-fluid d-flex justify-content-between align-items-center">
      <nav aria-label="breadcrumb">
        <ol class="breadcrumb" style="padding-left: 250px;">
          <li class="breadcrumb-item"><a href="/dashboard">Home</a></li>
          <li class="breadcrumb-item"><a href="/quick-help">Requests</a></li>
          <li class="breadcrumb-item active"><a >Request Details</a></li>
        </ol>
      </nav>
      <div class="user-profile">
        <img src="assets/user-avatar.png" alt="User" width="40" height="40" class="rounded-circle">
        <a href="/logout">Logout</a>
      </div>
    </div>
  </header>
  <div class="wrapper d-flex flex-column min-vh-100">
    <main class="container main-content">
      <div class="row">
        <div class="col-md-12">
          <div class="request-detail-card">
            <h2>Request Details</h2>
            <div class="request-info">
              <p><strong>Student:</strong> {{request.fullName}}</p>
              <p><strong>Email:</strong> {{request.email}}</p>
              <p><strong>Course:</strong> {{request.courseCode}} - {{request.courseName}}</p>
              <p><strong>Current Section:</strong> {{request.currentSection}}</p>
              <p><strong>Request Type:</strong> {{request.requestType}}</p>
              <p><strong>Reason:</strong> {{request.reason}}</p>
              <p><strong>Status:</strong> <span class="badge-pending">{{request.status}}</span></p>
              <textarea name="note" id="note" rows="3" placeholder="Add a note here..."></textarea>
            </div>
            
            <div class="action-buttons">
              
              <button class="btn btn-success" onclick="window.location.href='/approved?id={{request._id}}'">Approve</button>
              <button class="btn btn-danger" onclick="window.location.href='/rejected?id={{request._id}}'">Reject</button>
              <button class="btn btn-warning" onclick="window.location.href='/pending?id={{request._id}}'">Pending</button>
              <a href="/dashboard" class="btn btn-secondary">Back to Dashboard</a>
            </div>
          </div>
        </div>
      </div>
    </main>
  </div>

  
</body>
</html>